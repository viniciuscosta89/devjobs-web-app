import SearchFilterBarRoot from './SearchFilterBarRoot.vue';
import SearchFilterBarCheckbox from './SearchFilterBarCheckbox.vue';
import SearchFilterBarLocation from './SearchFilterBarLocation.vue';
import SearchFilterBarModal from './SearchFilterBarModal.vue';
import SearchFilterBarText from './SearchFilterBarText.vue';

const SearchFilterBar = {
  Root: SearchFilterBarRoot,
  Checkbox: SearchFilterBarCheckbox,
  Location: SearchFilterBarLocation,
  Modal: SearchFilterBarModal,
  Text: SearchFilterBarText
};

export default SearchFilterBar;
